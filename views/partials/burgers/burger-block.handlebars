<li class="burger">
	{{burger_name}}
    <button class="delete-burger btn  btn-sm btn-danger" data-id="{{this.id}}">DELETE!</button>

	{{#if devoured}}
        {{!-- do nothing --}}
    {{else}}
    <button class="devour-burger btn btn-sm btn-primary" data-id="{{this.id}}">DEVOUR IT!</button>
    {{/if}}
</li>


{{!-- {{#if sleep}}SLEEP TIME!{{else}}WAKE UP!{{/if}} --}}